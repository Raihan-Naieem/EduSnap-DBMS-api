{
  "name": "pern-dbms-backend",
  "version": "1.0.0",
  "description": "PERN stack backend for a DBMS project. Express + raw SQL via the pg package, no ORM.",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.6",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.12.0"
  }
}
